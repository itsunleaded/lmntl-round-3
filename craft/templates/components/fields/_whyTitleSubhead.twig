        <!--sectionTitleSubhead-->
        <section class="section sectionTitleSubhead">

            <!--Background-->
            {% set image = block.backgroundImage.one() %}
            <div class="section__image" {% if image %} style="background-image: url('{{ image.url }}');"{% endif %}></div>
            
            <div class="container h-100">
                <div class="row justify-content-center">
                    <div class="col-12 text-center">
                            {{ block.headline }}
                    </div>
                </div>
            </div>
        </section>
        <!--END-->